```
usage: devf.py [-h] --input INPUT --output OUTPUT

devf extracts EnCase file format (e01) files.

optional arguments:
  -h, --help       show this help message and exit
  --input INPUT
  --output OUTPUT
```
